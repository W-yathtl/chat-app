<div class="side-bar-header">
  <div class="header-name">
    <%# current_user.nameを表示し，クリックするとedit画面に遷移する。 %>
    <%= link_to current_user.name, edit_user_path(current_user) %>
  </div>
  <div class="create-room">
    <%= link_to "チャットを作成する", new_room_path %><%# チャットを作成するリンク %>
  </div>
</div>

<div class="rooms">
  <% current_user.rooms.each do |room| %><%# 現在のユーザーがs所属するroomsの数だけ処理を行う %>
    <div class="room">
      <div class="room-name">
         <%= link_to room.name, room_messages_path(room) %></a><%# room_nameを表示し，ルームとメッセージのネストのパスになっている。%>
      </div>
    </div>
  <% end %>
</div>